<section class="hero is-link is-small">
  <div class="hero-head">
    <nav class="navbar">
      <div class="container">
        <div class="navbar-brand">
          
          <a class="navbar-item">
            <%# <img src="https://bulma.io/images/bulma-type-white.png" alt="Logo"> %>
            <strong> FlyMsg </strong>
          </a>
          <% if !session[:user_id].nil? %>
            <div class="navbar-item">
              Welcome back, <%= session[:first_name] %>
            </div>
          <% end %>
          
          <span class="navbar-burger" data-target="navbarMenuHeroB">
            <span></span>
            <span></span>
            <span></span>
          </span>
        
        </div>
        <div id="navbarMenuHeroB" class="navbar-menu">
          <div class="navbar-end">
            <span class="navbar-item">

              <% if session[:user_id].nil? %>
                
                <div class="navbar-item">
                    <div class="buttons">
                      <a href="/create" class="button is-light">
                          <strong>Sign up</strong>
                      </a>
                      <a href="/login" class="button is-primary">
                          Log in
                      </a>
                    </div>
                </div>

              <% else %>

                <%= link_to "Encrypt", "/encrypt", class: "navbar-item" %>            
                <%= link_to "Decrypt", "/decrypt", class: "navbar-item" %>
                <%= link_to "Logout", "/logout", class: "navbar-item" %>

              
              <% end %>

            </span>
          </div>
        </div>
      </div>
    </nav>
  </div>
</section>
